name: "Sample Workflow"
on:
  workflow_dispatch:
    
jobs:
    run_edge:
        name: Test
        runs-on: self-hosted
        
        steps:
        - name: Run Hello World
          run: echo "Hello World1"
